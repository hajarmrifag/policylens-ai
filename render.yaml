services:
  - type: web
    name: policylens-ai
    runtime: docker
    plan: starter        # torch + MiniLM + FLAN-T5 need more than the 512 MB free tier
    healthCheckPath: /health
    autoDeploy: true
